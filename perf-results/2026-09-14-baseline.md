# Performance Baseline — 2026-09-14

## Environment

- Machine: MacBook (Apple Silicon)
- Node: v22.18.0
- pnpm: 10.23.0
- vitest: v4.0.18

## Evaluator benchmark (`pnpm bench`)

| Case                    | mean (ms) | p99 (ms) | cloneDeep calls (analytical) |
|-------------------------|-----------|----------|------------------------------|
| 1k  features × 5  expr  |     6.31  |    6.98  | 5,000                        |
| 10k features × 5  expr  |    65.77  |   72.51  | 50,000                       |
| 10k features × 10 expr  |   118.09  |  124.37  | 100,000                      |
| 10k features × 15 expr  |   177.81  |  180.81  | 150,000                      |
| 35k features × 10 expr  |   418.78  |  438.89  | 350,000                      |

### Scaling proof (confirms per-expression cloning)

| Ratio                              | Observed | Expected if per-expression | Expected if per-feature |
|------------------------------------|----------|---------------------------|------------------------|
| mean(10k × 15) / mean(10k × 5)    |    2.70× |                      3.0× |                    1.0× |
| mean(10k × 10) / mean(10k × 5)    |    1.80× |                      2.0× |                    1.0× |

Ratios track linearly with expression count → confirms `cloneDeep` is called per expression, not per feature.

### What this means

Call chain: `evalSimpleLayer` → `evalSimpleLayerFeature` per feature → `evalLayerAppearances` →
`recursiveValEval` → `evalExpression` (×exprCount per feature) → `cloneDeep(feature)`.

Total `cloneDeep` calls = features × expressions. For a 35k-feature layer with 10 style expressions:
**350,000 deep clones per render cycle**.

After **P1 fix** (hoist clone to feature level): 35,000 clones → **10× reduction**.
Expected mean improvement for `35k × 10`: ~418ms → ~42ms.

## MVT reference stability (`pnpm test mvt.reference`)

| Metric                                       | Baseline | Expected after P2a fix |
|----------------------------------------------|----------|------------------------|
| `extractSimpleLayer` same ref on repeat call | NO       | YES                    |
| Provider constructions in 10 re-renders      | 10       | 0                      |

### What this means

`extractSimpleLayer` calls `toPlainObject = cloneDeep(obj)` on every invocation.
`useMVT` (mvt.ts:19) calls it on every render and puts the result in `useMemo`'s dep array.
Since the reference changes every render, `useMemo` never caches → `MVTImageryProvider` is
reconstructed on every render, even when no props changed.

After **P2a fix**: stabilize the reference (e.g. deep-equal comparison or JSON-stable serialization)
so `useMemo` actually caches the provider.

## Notes

- Bench machine is lightly loaded during measurement (no browser/Cesium running)
- `cloneDeep` call counts are derived analytically from the call graph (not instrumented at runtime)
- The `vitest bench` command is experimental and means may vary ±5% run-to-run
